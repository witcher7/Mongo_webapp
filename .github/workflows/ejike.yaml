name: random-workflow
on: 
   workflow_dispatch
jobs:
    hello-job:
         runs-on: ubuntu-latest
         steps:
           - name: Print Hello
             run: echo "Hello"
    # runners --> windows, macos , ubuntu
  
